// Alterando estilos
const element = document.querySelector('body');

// definindo valor da propriedade 
element.style.backgroundColor = '#f9f302';

// exibindo valor da propriedade definida
console.log(element.style.backgroundColor);